# Nest.js / GraphQL / Mongo

## Instructions
First create `.env` file
```bash
touch .env
```

Then, Fill it with the following:
```bash
DATABASE_URL=""
JWT_SECRET=""
JWT_EXPIRATION="24h"
SALT_ROUND="8"
```

## Run
```
npm run install
npm run start:dev
```